import { RenderComponentDirective } from './render-component.directive';

describe('RenderComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderComponentDirective();
    expect(directive).toBeTruthy();
  });
});
